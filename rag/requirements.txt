# =============================================================================
# Cockpit Financeiro Estratégico — dependências dos scripts RAG
# =============================================================================
# Python 3.10+ recomendado.
#
#   pip install -r requirements.txt
#
# psycopg (v3) é o driver preferido; psycopg2 é aceito como fallback pelos
# scripts (embed.py / ask.py detectam qual está disponível).
# O SDK anthropic é opcional — ask.py usa HTTP cru via urllib se ausente.
# =============================================================================

# Driver PostgreSQL (v3). Os scripts também funcionam com psycopg2-binary.
psycopg[binary]>=3.1

# SDK oficial da Anthropic (opcional — fallback HTTP embutido em ask.py).
anthropic>=0.49

# Carregamento de variáveis de ambiente a partir de .env (opcional, mas útil).
python-dotenv>=1.0
