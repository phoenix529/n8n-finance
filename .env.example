# Cockpit REF — modelo de variáveis de ambiente (Blueprint §4.4).
# Copie para `.env` e preencha. NÃO versione o .env (está no .gitignore).
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=cockpit_ref
DB_USER=cockpit_user
DB_PASSWORD=<senha-forte-do-banco>
ANTHROPIC_API_KEY=<sua-chave-anthropic>
LLM_MODEL=claude-opus-4-8
IA_API_KEY=<chave-da-api-de-ia>
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=<senha-forte-n8n>
# Fase 2 (COR): preencher quando o cliente fornecer
# COR_BASE_URL=https://api.cor...
# COR_API_TOKEN=<token-do-cliente>
